Dashboard might have two things reconilations run and vendor risk.
Reconcilation UI which will have two CTAs upload the invoice list csv and uplod the csv download list from gstr 2B.
On clicking reconcile we will redirect to a list of the uploaded records and summary of the categories into which the records are grouped like low risk (typos), high risk (not filed), cannot be filed (these cannot be claimed) These categories will work like a filter. list columns includes columns in csv plus status, action and summary(a markdown generated by AI)
Can also download the data as csv with filter applied.
List of previously run reconcilations. On clicking an item in the list redirect to previous page(Reconcilation UI) Son Reconcilation UI can be with a runid(already ran ) or without id(creating new run)